"""API request/response models.

Prefer reusing depict.common.schemas (AssayRecord, Classification) over redefining
record shapes here; add app-only view models only when the API needs to diverge.
"""
